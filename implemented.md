# Implemented Features

- Upgraded the managed React Native app in `mobile/` to Expo SDK 57, including the compatible React Native, React, Expo Router, and Expo native module versions.
- Migrated the splash screen configuration to the `expo-splash-screen` config plugin required by the SDK 57 app configuration schema.
